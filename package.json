{
  "name": "terranejs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Wall-mountable 3D-printed topography tiles — library + website.",
  "scripts": {
    "dev": "python3 scripts/dev-server.py",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "@types/node": "^22",
    "@types/three": "^0.185.0",
    "@xmldom/xmldom": "^0.9.10",
    "typescript": "^5.6.0"
  }
}
